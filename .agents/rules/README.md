# Rules

Rules are deeper reusable constraints loaded only when relevant.

Current rules:

- `context-loading.md` — minimize context while loading enough design knowledge for the task.
- `verification.md` — evidence classes, browser-dependent claims, trustworthy gates, and critique separation.

Phase 2 will add adapted rules backed by the imported token, component, accessibility, taste, and framework knowledge layers.
